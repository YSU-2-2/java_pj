# RCW 프로젝트 진행상황

## Week 1 - Day 1 (11/21): AWS RDS 연결 + 로그인/회원가입 API

### 1. AWS RDS 설정
#### 1.1 RDS 인스턴스 생성
- [ ] AWS 콘솔 접속 → RDS 서비스 이동
- [ ] MySQL 8.0 데이터베이스 생성
  - 인스턴스 타입: db.t3.micro (프리티어)
  - 스토리지: 20GB
  - 퍼블릭 액세스: 예
- [ ] 보안 그룹 설정
  - 인바운드 규칙: MySQL(3306) 포트 개방
  - 소스: 내 IP 또는 0.0.0.0/0 (개발용)
- [ ] 엔드포인트 URL 확인 및 저장

#### 1.2 데이터베이스 및 테이블 생성
```sql
-- 데이터베이스 생성
CREATE DATABASE rcw_db;
USE rcw_db;

-- users 테이블
CREATE TABLE users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    nickname VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- rooms 테이블
CREATE TABLE rooms (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    room_code VARCHAR(20) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- saved_drawings 테이블
CREATE TABLE saved_drawings (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    room_code VARCHAR(20) NOT NULL,
    user_id BIGINT NOT NULL,
    s3_url VARCHAR(500) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (room_code) REFERENCES rooms(room_code)
);
```

작업 체크리스트:
- [ ] MySQL Workbench 또는 CLI로 RDS 접속 테스트
- [ ] 위 SQL 스크립트 실행하여 테이블 생성
- [ ] 테이블 생성 확인 (`SHOW TABLES;`)

### 2. Spring Boot - RDS 연결

#### 2.1 의존성 추가 (pom.xml 또는 build.gradle) ✅ 완료
```xml
<!-- MySQL Driver -->
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
</dependency>

<!-- Spring Data JPA -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>
```
- [x] pom.xml에 의존성 추가 완료 (11/21)

#### 2.2 application.properties 설정
```properties
# RDS 연결 정보
spring.datasource.url=jdbc:mysql://[RDS_ENDPOINT]:3306/rcw_db
spring.datasource.username=[DB_USERNAME]
spring.datasource.password=[DB_PASSWORD]
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
```

작업 체크리스트:
- [ ] `backend/src/main/resources/application.properties` 파일 수정
- [ ] RDS 엔드포인트, 사용자명, 비밀번호 입력
- [ ] Spring Boot 애플리케이션 실행하여 연결 테스트

### 3. 로그인/회원가입 API 구현

#### 3.1 Entity 클래스 생성
파일: `backend/src/main/java/com/rcw/entity/User.java`
```java
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false)
    private String username;

    @Column(nullable = false)
    private String password;

    @Column(nullable = false)
    private String nickname;

    private LocalDateTime createdAt;

    // Getter, Setter, Constructor
}
```

#### 3.2 Repository 인터페이스
파일: `backend/src/main/java/com/rcw/repository/UserRepository.java`
```java
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    boolean existsByUsername(String username);
}
```

#### 3.3 DTO 클래스
파일: `backend/src/main/java/com/rcw/dto/`
```java
// LoginRequest.java
public class LoginRequest {
    private String username;
    private String password;
}

// SignupRequest.java
public class SignupRequest {
    private String username;
    private String password;
    private String nickname;
}

// AuthResponse.java
public class AuthResponse {
    private Long userId;
    private String username;
    private String nickname;
    private String message;
}
```

#### 3.4 Service 클래스
파일: `backend/src/main/java/com/rcw/service/AuthService.java`
```java
@Service
public class AuthService {
    @Autowired
    private UserRepository userRepository;

    // 회원가입
    public AuthResponse signup(SignupRequest request) {
        // 1. 중복 체크
        if (userRepository.existsByUsername(request.getUsername())) {
            throw new RuntimeException("이미 존재하는 아이디입니다");
        }

        // 2. 사용자 생성
        User user = new User();
        user.setUsername(request.getUsername());
        user.setPassword(request.getPassword()); // TODO: 암호화 필요
        user.setNickname(request.getNickname());
        user.setCreatedAt(LocalDateTime.now());

        User savedUser = userRepository.save(user);

        return new AuthResponse(savedUser.getId(), savedUser.getUsername(),
                                savedUser.getNickname(), "회원가입 성공");
    }

    // 로그인
    public AuthResponse login(LoginRequest request) {
        User user = userRepository.findByUsername(request.getUsername())
            .orElseThrow(() -> new RuntimeException("존재하지 않는 사용자입니다"));

        if (!user.getPassword().equals(request.getPassword())) {
            throw new RuntimeException("비밀번호가 일치하지 않습니다");
        }

        return new AuthResponse(user.getId(), user.getUsername(),
                                user.getNickname(), "로그인 성공");
    }
}
```

#### 3.5 Controller 클래스
파일: `backend/src/main/java/com/rcw/controller/AuthController.java`
```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {
    @Autowired
    private AuthService authService;

    @PostMapping("/signup")
    public ResponseEntity<AuthResponse> signup(@RequestBody SignupRequest request) {
        try {
            AuthResponse response = authService.signup(request);
            return ResponseEntity.ok(response);
        } catch (RuntimeException e) {
            return ResponseEntity.badRequest()
                .body(new AuthResponse(null, null, null, e.getMessage()));
        }
    }

    @PostMapping("/login")
    public ResponseEntity<AuthResponse> login(@RequestBody LoginRequest request) {
        try {
            AuthResponse response = authService.login(request);
            return ResponseEntity.ok(response);
        } catch (RuntimeException e) {
            return ResponseEntity.badRequest()
                .body(new AuthResponse(null, null, null, e.getMessage()));
        }
    }
}
```

#### 3.6 작업 체크리스트
- [ ] Entity 클래스 생성 (User.java)
- [ ] Repository 인터페이스 생성 (UserRepository.java)
- [ ] DTO 클래스 생성 (LoginRequest, SignupRequest, AuthResponse)
- [ ] Service 클래스 생성 (AuthService.java)
- [ ] Controller 클래스 생성 (AuthController.java)
- [ ] Postman으로 API 테스트
  - POST `/api/auth/signup` - 회원가입 테스트
  - POST `/api/auth/login` - 로그인 테스트

### 4. API 테스트 예시

#### 회원가입 테스트
```json
POST http://localhost:8080/api/auth/signup
Content-Type: application/json

{
    "username": "testuser",
    "password": "1234",
    "nickname": "테스터"
}

예상 응답:
{
    "userId": 1,
    "username": "testuser",
    "nickname": "테스터",
    "message": "회원가입 성공"
}
```

#### 로그인 테스트
```json
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
    "username": "testuser",
    "password": "1234"
}

예상 응답:
{
    "userId": 1,
    "username": "testuser",
    "nickname": "테스터",
    "message": "로그인 성공"
}
```

### 5. 완료 조건
- [x] AWS RDS MySQL 인스턴스 생성 완료
- [ ] RDS에 데이터베이스 및 테이블 생성 완료
- [x] Spring Boot 프로젝트 생성 완료 (11/21)
- [x] pom.xml 의존성 확인 완료 (MySQL, JPA, WebSocket 모두 포함)
- [ ] application.properties에 RDS 연결 설정 추가 (내일 진행)
- [ ] Spring Boot 애플리케이션이 RDS에 정상 연결
- [ ] 회원가입 API 정상 작동 (DB에 데이터 저장 확인)
- [ ] 로그인 API 정상 작동 (인증 성공/실패 확인)
- [ ] Postman 또는 클라이언트에서 API 호출 테스트 완료

---

## 진행 상황 요약 (11/21)

### ✅ 완료된 작업
1. **Spring Boot 백엔드 프로젝트 생성**
   - 프로젝트 위치: `rcw-backend/`
   - Maven 프로젝트 (pom.xml)
   - Java 21 사용
   - 패키지명: `com.drawgather.rcw_backend`
   - 메인 클래스: `RcwBackendApplication.java`

2. **필요한 의존성 모두 추가 완료**
   - ✅ spring-boot-starter-web (REST API용)
   - ✅ spring-boot-starter-data-jpa (DB 연동용)
   - ✅ mysql-connector-j (MySQL 드라이버)
   - ✅ spring-boot-starter-websocket (실시간 통신용)
   - ✅ spring-boot-devtools (개발 편의성)

3. **프로젝트 구조 확인**
   ```
   rcw-backend/
   ├── pom.xml (의존성 설정 파일)
   ├── src/
   │   ├── main/
   │   │   ├── java/com/drawgather/rcw_backend/
   │   │   │   └── RcwBackendApplication.java
   │   │   └── resources/
   │   │       ├── application.properties (아직 비어있음)
   │   │       ├── static/
   │   │       └── templates/
   │   └── test/
   ```

### 📋 다음 작업 (11/22 예정)

#### 2단계: application.properties 설정 (RDS 연결)
파일 위치: `rcw-backend/src/main/resources/application.properties`

**추가할 내용:**
```properties
# 애플리케이션 이름
spring.application.name=rcw-backend

# RDS 연결 정보
spring.datasource.url=jdbc:mysql://[RDS_ENDPOINT]:3306/rcw_db
spring.datasource.username=[DB_USERNAME]
spring.datasource.password=[DB_PASSWORD]
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 서버 포트 (선택사항, 기본값 8080)
server.port=8080
```

**작업 순서:**
1. AWS RDS 엔드포인트 URL 확인
2. RDS 접속 정보 확인 (username, password)
3. application.properties에 위 설정 추가
4. Spring Boot 실행하여 연결 테스트
   ```bash
   cd rcw-backend
   ./mvnw spring-boot:run
   ```
5. 콘솔에 에러 없이 실행되면 성공

#### 3단계: 로그인/회원가입 API 구현
- Entity, Repository, Service, Controller 클래스 생성
- API 엔드포인트 구현
- Postman으로 테스트

### 참고사항
- 비밀번호는 현재 평문 저장 (추후 BCrypt 암호화 적용 권장)
- 세션/토큰 관리는 추후 구현 (현재는 기본 인증만)
- CORS 설정 필요시 `@CrossOrigin` 또는 WebConfig 설정 추가

---

## 다음 단계 (Week 1 - Day 2)
- Java Swing UI 기본 화면 구현
- 로컬 드로잉 기능 (Canvas, 펜, 색상, 지우개)
